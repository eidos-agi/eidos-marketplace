# Banned / high-suspicion vocabulary (composite)

**Regenerated by** `scripts/merge-lexicon.py`.
Format: `term  # sources: a, b`

## Words

```
although  # sources: adewale
and  # sources: adewale
as we have seen  # sources: house
ask-author  # sources: adewale
at the end of the day  # sources: house
beacon  # sources: house, petergyang
because  # sources: adewale
concrete rewrite  # sources: adewale
cutting-edge  # sources: house, jalaalrd, petergyang
delve  # sources: adewale, house, jalaalrd, petergyang
empower  # sources: adewale, house, jalaalrd, petergyang
features  # sources: adewale
format: `term  # sources: a, b`, house|jalaalrd|adewale|petergyang`
foster  # sources: adewale, house, jalaalrd, petergyang
furthermore  # sources: house (throat-clearing)
game-changer  # sources: house, jalaalrd
harness  # sources: house (figurative), jalaalrd, petergyang
holistic  # sources: house
in conclusion  # sources: house
in today's fast-paced  # sources: house, jalaalrd
interplay  # sources: house, jalaalrd
it is important to note  # sources: house
it's not just  # sources: house, jalaalrd
it's worth noting  # sources: house
landscape  # sources: adewale, house, jalaalrd, jalaalrd  (override: allow in IC market maps — see house-overrides)
leverage  # sources: house (figurative), jalaalrd, petergyang
marks  # sources: adewale
moreover  # sources: house (throat-clearing)
multifaceted  # sources: adewale, house, jalaalrd, petergyang
no bold random phrases  # sources: jalaalrd
no markdown headers  # sources: jalaalrd
no markdown in plain text contexts  # sources: jalaalrd
not only  # sources: house (when paired with "but also" padding)
once  # sources: adewale
passes self-detectors  # sources: adewale
pivotal  # sources: adewale, house, jalaalrd, petergyang
realm  # sources: adewale, house, jalaalrd, petergyang
represents  # sources: adewale
resonate  # sources: house
revise  # sources: adewale
rewrite check  # sources: adewale
robust  # sources: adewale, house (empty praise), jalaalrd, petergyang
seamless  # sources: adewale, house, jalaalrd
serves  # sources: adewale
serves as  # sources: adewale
so that  # sources: adewale
stands as  # sources: adewale
tapestry  # sources: adewale, house, jalaalrd, petergyang
testament  # sources: adewale, house, jalaalrd, petergyang
that was the point  # sources: adewale
underscore  # sources: adewale, house, jalaalrd, petergyang
utilize  # sources: adewale, house, house → use, jalaalrd, petergyang
vibrant  # sources: house, jalaalrd
what changed  # sources: petergyang
where  # sources: adewale
whether you're a beginner or an expert  # sources: adewale
which means  # sources: adewale
yet  # sources: adewale
```

## See also

- `house-overrides.md` — technical/IC exceptions
- `UPSTREAM.md` — vendor policy

